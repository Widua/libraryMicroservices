server:
  port: 8080

spring:
  application:
    name: bookMicroservices

  jpa:
    database: postgresql
    generate-ddl: true
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: user
    password: passwd

