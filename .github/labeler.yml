# Add 'tests' label to any change in tests
tests:
  - '**/test/**/*'
  - '**/test/**'

# Add 'actions' label to any change within the github actions
actions:
- .github/**

# Add 'Book Microservice' label to any change to src files within the bookMicroservice
Book Microservice:
- bookMicroservice/**
